# Steps
1. Install Maven 3 before running the run.sh script
2. Make the run.sh script executable:
```
   chmod +x run.sh
```
3. Run the run.sh script:
```
   ./run.sh
```
	This will compile and run the application.

4. Go to http://localhost:8081/hello
